# Network Ping Pro has no runtime Python dependencies.
# It uses only the Python standard library.
#
# For the HTTP method, install curl separately:
#   - Windows 10+: preinstalled, or https://curl.se/windows/
#   - Linux: sudo apt install curl
#   - macOS: preinstalled